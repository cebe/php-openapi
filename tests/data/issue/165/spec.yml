openapi: 3.0.3
info:
  title: Test
  version: 0.1.0
paths:
  /test:
    get:
      responses:
        "200":
          description: Test

components:
  schemas:
    answer:
      type: object
      properties:
        id:
          type: integer
          summary: Unique answer ID
