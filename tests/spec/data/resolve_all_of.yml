
openapi: 3.0.3

info:
  title: Resolve allOf
  version: 1.0.0

components:
  schemas:
    User:
      type: object
      required:
        - id
        - name
      properties:
        id:
          type: integer
        name:
          type: string
    Pet:
      type: object
      required:
        - id2
        - name2
      properties:
        id2:
          type: integer
        name2:
          type: array
    Post:
      type: object
      properties:
        id:
          type: integer
        content:
          type: string
        user:
          allOf:
            - $ref: '#/components/schemas/User'
            - $ref: '#/components/schemas/Pet'
            - x-faker: false
            - x-faker2: true

paths:
  '/':
    get:
      responses:
        '200':
          description: OK
